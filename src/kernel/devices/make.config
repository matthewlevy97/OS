# This file purposefully has no includes
# To include a driver in the kernel, just add the drivers 'make.config'
# For standarization, ensure all 'make.config' files save objects to variable
#   named [DEVICE_FOLDER] + "_OBJS" (ex. "8259PIC_OBJS")